Tive alguns problemas e não pude terminar, basicamente fiz apenas a listagem na tabela mesmo :/

## Pra visualizar:

`git clone https://github.com/eliasinacio/top-musics.git`

e na raiz do projeto

`yarn start` / `npm start`
